<%# .erb firles are a combination of HTML and ruby. These fules are organized based on the controller to which
they correspond. There is a view fule for each controller action %>

<% if notice %>
<aside id="notice"><%= notice %></aside>
<% end %>
<h1>Yash's awesome catalog</h1>
<ul class="catalog">
<% @products.each do |product| %>
<li>
<%= image_tag(product.image_url) %> <h2><%= product.title %></h2>
<p>
<%= sanitize(product.description) %> </p>
<div class="price"> <%= product.price %>
</div> </li>
<% end %> </ul>

